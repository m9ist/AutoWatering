# AutoWatering

## Agent skills

### Issue tracker

Issues live in GitHub Issues (m9ist/AutoWatering) via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context: `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
